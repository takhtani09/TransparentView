    import XCTest
    @testable import TransparentView

    final class TransparentViewTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(TransparentView().text, "Hello, World!")
        }
    }
